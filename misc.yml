---
- hosts: localhost
  gather_facts: true
  tasks:
    - name: Do misc command in shell
      ansible.builtin.shell: |
        ls -la
        which python
        pwd
        python -V
        python3 -V
        exit 0
      
